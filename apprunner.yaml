version: 1.0
runtime: nodejs12
build:
  commands:
    build:
      - npm install --production
run:
  command: npm start -- "npm run testdrive -- ./package/frontend/ethereum/backend/output/server.js"
  network:
    port: 8080
