{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "ts-lint": "read -p \"`$HOME/.nvm/versions/node/v16.4.0/bin/npx ts-standard --verbose --fix`\"",
    "clasp": "$HOME/.nvm/versions/node/v16.4.0/bin/npx concurrently --kill-others-on-fail --restart-tries 0 -n LINT,CODE -c grey,blue \"npm run ts-lint\" \"NODE_ENV=development $HOME/.nvm/versions/node/v16.4.0/bin/clasp push --watch\"",
    "type": "$HOME/.nvm/versions/node/v16.4.0/bin/npx tsc -w",
    "start": "$HOME/.nvm/versions/node/v16.4.0/bin/npx concurrently --kill-others-on-fail --restart-tries 0 -n COMPILER,CODE -c grey,blue \"npm run type\" \"npm run clasp\""
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.37",
    "typescript": "^3.8.2"
  },
  "dependencies": {
    "web3": "^1.6.0"
  }
}
