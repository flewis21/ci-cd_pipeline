{
  // files
  "files": ["./package/backend/src/server.ts"],

  // exclude
  "exclude": [],

  //references
  "references": [
    // reference points
    {
      // Passes control to backend tsconfig-output
      "path": "./package/backend/tsconfig-output.json"
    },
    {
      // Passes control to ethereum tsconfig
      "path": "./package/ethereum/tsconfig.json"
    },
    {
      // Passes control to frontend tsconfig
      "path": "./package/frontend/tsconfig.json"
    },
    {
      // Passes control to sandbox tsconfig
      "path": "./package/sandbox/tsconfig.json"
    },
    {
      // Passes control to simpleGit tsconfig
      "path": "./package/simpleGit/tsconfig.json"
    }
  ]
}
